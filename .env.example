# Copy this file to .env and fill in your real values.
# NEVER commit the real .env file to git — add .env to .gitignore.

HASHBACK_BASE_URL=https://api.hashback.co.ke
HASHBACK_API_KEY=4db95a6d3c97a7bc473ed1d6f44502cc124e2556936051ae88699bc9834b9d12
HASHBACK_ACCOUNT_ID=HP502244

# Where HashBack should send async payment confirmations.
# Must be a public HTTPS URL once deployed, e.g.:
# https://your-app-name.onrender.com/api/hashpay-callback
CALLBACK_URL=https://your-deployed-backend.example.com/api/hashpay-callback

# Comma-separated list of allowed web origins, e.g.:
# https://your-flutter-app.web.app,https://your-flutter-app.firebaseapp.com
# Use * only for local testing.
ALLOWED_ORIGINS=*

PORT=3000
