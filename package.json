{
  "name": "hashpay-proxy",
  "version": "1.0.0",
  "description": "Server-side proxy for HashBack HashPay so the Flutter web app never calls HashBack or holds the API key directly.",
  "main": "server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
